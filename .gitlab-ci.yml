image: docker:dind

services:
  - docker:dind

postman_tests:
  script:
  - docker run -v $(pwd)/tests:/etc/newman -t postman/newman:latest run "simd-visualizer-tests.postman_collection.json" --reporters="cli"
  stage: test